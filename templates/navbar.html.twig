 <nav class="navbar navbar-expand-xl navbar-dark bg-primary">
                <div class="d-flex container-fluid my-1">
                      
                           <div class="m-0 p-0"> 
                                <div class="navbar-brand m-3 m-auto">
                                                  <a class="navbar-brand" href="{{path('app_home')}}">
                                             <img src="{{asset('/images/navicon.png')}}" width="30" height="30" class="" alt="">
                                                                                 Shrinkyz
                                                        </a>
                                </div>
                            </div>


                                 <div class="mr-auto">
                                        {{app.request.get('_route') == 'app_home' ?  '   <button class="btn btn-primary special-button btn-sm " id="menu-toggle">Categories </button>' }}
                                        {{app.request.get('_route') == 'link_category' ?  '    <button class="btn btn-primary special-button btn-sm " id="menu-toggle">Categories </button>' }}
                                </div> 

                                <div class="ml-auto">
                                    <button aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler" data-target="#navbarColor01" data-toggle="collapse" type="button">
                                                <span class="navbar-toggler-icon"></span>
                                          </button>
                                
                                </div>

                            <div class="collapse navbar-collapse" id="navbarColor01">
                    
                  
                                <div class="ml-5">
                            
                            
                                    <ul class="navbar-nav">
                                        <li class="nav-item {{ app.request.get('_route') == 'app_home' ? 'active' }}">
                                            <a class="nav-link " href="{{path('app_home')}}">Home</a>
                                           
                                         
                                           
                                        </li>
                                            {# remove this in production coz this only for dev #}
                                            {# <li class="nav-item {{ app.request.get('_route') == 'about' ? 'active' }}">
                                            <a class="nav-link" href="{{ path('dashboard') }}">ADMINTEST</a>
                                        </li> #}
                                            {# until this line #}




                                        <li class="nav-item {{ app.request.get('_route') == 'about' ? 'active' }}">
                                            <a class="nav-link" href="{{ path('about') }}">About</a>
                                        </li>
                                        <li class="nav-item {{ app.request.get('_route') == 'contact' ? 'active' }}">
                                            <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                                        </li>
                                    </ul>
                                                    
                                                    {#  not suitable for production go to /registeradmin
                                                    <li class="nav-item {{ app.request.get('_route') == 'dashboard' ? 'active' }}">
                                                        <a class="nav-link" href="{{ path('dashboard') }}">Admin</a>
                                                    </li>
                                                    <li class="nav-item {{ app.request.get('_route') == 'register_admin' ? 'active' }}">
                                                        <a class="nav-link" href="{{ path('register_admin') }}">Admin Register</a>
                                                    </li> #}
                                </div>      
                            <div class="m-auto">    


                                 {{render(controller('App\\Controller\\SearchBarController::index'))}}
                                        
                            
                                                    
                            </div>
                            
                            <div class="ml-auto">
                                <ul class="navbar-nav">
                                        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                                            <li class="nav-item {{ app.request.get('_route') == 'app_login' ? 'active' }}">
                                                <a class="nav-link  " href="{{ path('app_login') }}">Login</a>
                                            </li>

                                            <li class="nav-item {{ app.request.get('_route') == 'app_register' ? 'active' }}">
                                                <a class="nav-link " href="{{ path('app_register') }}">Register</a>
                                            </li>

                                        {% endif %}
                                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                            <li class="nav-item dropdown ">
                                                <a aria-expanded="true" aria-haspopup="true" class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button">{{ app.user.email }}</a>
                                                <div class="dropdown-menu " style="position: absolute; will-change: transform; top: 0px; left: 0px; transform: translate3d(0px, 38px, 0px);" x-placement="bottom-start">
                                                    {% if is_granted('ROLE_ADMIN') %}
                                                    <a class="dropdown-item" href=" {{ path('dashboard') }}">Admin</a>
                                                     {% endif %} 
                                                    <a class="dropdown-item" href="{{path('user_profile')}}">My Profile</a>
                                                    <a class="dropdown-item" href="{{path('user_orders')}}">My Orders</a>
                                                    <a class="dropdown-item" href=" {{ path('app_logout') }}">Logout</a>
                                        {% endif %}
                                        

                                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                                </div>
                                            </li>

                                            <li class="nav-item">
                                                <a class="nav-link" href="{{path('view_cart')}}">My Cart
                                                    {{render(controller('App\\Controller\\UserController::cartNote'))}}</a>
                                            </li>


                                        {% endif %}
                                </ul>
                              
                            </div>                 
                    
                    
                    
                        
                    </div>{# collpase end#}
                </div> {# container end#}
</nav>