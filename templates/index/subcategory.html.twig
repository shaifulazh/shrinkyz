{% extends '/index/main.html.twig' %}
{% block title %}Welcome{% endblock %}

{% block head %}
    {{ parent() }}
{% endblock %}


{% block main %}

    <div class="mt-3">
    
    <div class="container">

    <div class="row">
    <div class="ml-3">
    {% if subtwocategories  is defined %}
    {% for subtwocategory in subtwocategories %}
    </div>
    </div>
        
        <a href="{{path('subtwocategories_link', {id : subtwocategory.id})}}" class="btn btn-sm btn-info">{{ subtwocategory.subtwocategoryname }}</a>
    {% endfor %}
    {% endif %}
{{include('index/product.html.twig')}}
    </div>
    
    </div>
    
  


{% endblock %}
