{% extends '/index/main.html.twig' %}
{% block title %}Shrinkyz{% endblock %}

{% block head %}
    {{ parent() }}
{% endblock %}


{% block main %}

    <div class="mt-3">
    
    <div class="container">

    <div class="row">
    <div class="ml-3">

 
  


    <ul class="nav nav-tabs">
    {% if subtwocategories  is defined %}
    {% for subtwocategory in subtwocategories %}
        
  <li class="nav-item">
        <a href="{{path('subtwocategories_link', {id : subtwocategory.id})}}" class="nav-link"> {{ subtwocategory.subtwocategoryname }}</a>
  </li>
    {% endfor %}
    {% endif %}
</ul>
    </div>
    </div>
{{include('index/product.html.twig')}}
    </div>
    
    </div>
    
  


{% endblock %}
