{% extends '/index/main.html.twig' %}
{% block title %}Welcome{% endblock %}



{% block head %}
    {{ parent() }}

    {# <style>
        .active-{{app.request.get('id')}}{
            background: #dadada !important;
        }
    </style> #}

{% endblock %}


{% block main %}

    <div class="mt-3">
    
        <div class="container">

                    <div class="row">
                <div class="ml-3">
                {% if subcategories  is defined %}
                {% for subcategory in subcategories %}
                    
                    <a href="{{path('subcategories_link', {id : subcategory.id})}}" class="btn btn-sm btn-info">{{ subcategory.subcategoryname }}</a>
                {% endfor %}
                {% endif %}
                </div>
                </div>
            {{include('index/product.html.twig')}}
        </div>
    
    </div>

{% endblock %}
