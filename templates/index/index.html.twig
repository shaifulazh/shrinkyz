{% extends '/index/main.html.twig' %}
{% block title %}Welcome{% endblock %}



{% block head %}
    {{ parent() }}

    {# <style>
        .active-{{app.request.get('id')}}{
            background: #dadada !important;
        }
    </style> #}

{% endblock %}


{% block main %}

    <div class="mt-3">
    
        <div class="container">

                    <div class="row">
                <div class="ml-3">
                <ul class="nav nav-tabs">
                {% if subcategories  is defined %}
                {% for subcategory in subcategories %}
                     <li class="nav-item">
                    <a href="{{path('subcategories_link', {id : subcategory.id})}}" class="nav-link"> {{ subcategory.subcategoryname }}</a>
                    </li>
                {% endfor %}
                {% endif %}
                </ul>
                </div>
                </div>
            {{include('index/product.html.twig')}}
        </div>
    
    </div>


     <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script>
        $("#menu-toggle").click(function (e) {
            e.preventDefault();
            $("#wrapper").toggleClass("toggled");
        });


        function reqfunction(e){
            e.preventDefault();
            console.log('someone click')
        }

   


        {# $(document).ready(function() { 
            
             if( /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) ) {
                  $("#wrapper").toggleClass("toggled");
            console.log('this is mobile');
 
}

         }); #}

{#        
window.onload = (event) => {
 
   if( /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) ) {
                  $("#wrapper").toggleClass("toggled");
            console.log('onload');
 
}
  
}; #}
        
    </script>

{% endblock %}
